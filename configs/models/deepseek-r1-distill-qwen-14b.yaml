# DeepSeek-R1-Distill-Qwen-14B — Chinese reasoning model
name: deepseek-r1-distill-qwen-14b
hf_id: deepseek-ai/DeepSeek-R1-Distill-Qwen-14B
revision: null  # TODO: pin after first download
# Local Apple-Silicon path: uncomment `backend: mlx` to attack/eval on the laptop via
# the torch-free MLX backend. Default "hf" keeps the CUDA reproduction path.
# backend: mlx
mlx_id: mlx-community/DeepSeek-R1-Distill-Qwen-14B-4bit  # confirm exact id on hf.co/mlx-community
family: deepseek
params_billion: 14.0
# NOTE: use_quantization governs the HF/CUDA path ONLY. The MLX path's precision is set
# by mlx_id above (4-bit); see the quantization control (prereg A-2/H7) for the fp16 arm.
use_quantization: false  # HF/CUDA path: fp16
use_flash_attn: true
trust_remote_code: false
max_seq_len: 4096
notes: |
  Distilled reasoning model from DeepSeek R1. Topical (R1 family was the
  centerpiece of the 2025 safety discussion). Critically: the model exposes
  its chain-of-thought in <think>...</think> tags, which constitutes a
  novel attack surface that non-reasoning models lack.

  The published model card documents below-baseline refusal rates on
  several harm categories — making this our most fragile-by-default
  starting point.
